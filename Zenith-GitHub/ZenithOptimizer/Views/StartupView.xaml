<UserControl x:Class="Zenith.Views.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer Style="{StaticResource PageScroll}">
        <StackPanel Margin="0,0,4,32">
            <!-- Header -->
            <Grid Margin="0,4,0,24">
                <StackPanel Margin="0,0,180,0">
                    <TextBlock Text="Startup Apps" Style="{StaticResource H1}"/>
                    <TextBlock Text="Every app here launches when you sign in. Turning off the ones you don't need right away makes boot noticeably faster. Changes also show in Task Manager." Style="{StaticResource Subtitle}"/>
                </StackPanel>
                <Button Style="{StaticResource SecondaryButton}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="Refresh_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE72C;" FontFamily="{StaticResource IconFont}" FontSize="13" VerticalAlignment="Center"/>
                        <TextBlock Text="Refresh" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>

            <!-- Summary -->
            <Border Style="{StaticResource Card}" Padding="24,18" Margin="0,0,0,16">
                <DockPanel>
                    <Control x:Name="Spinner" DockPanel.Dock="Right" Style="{StaticResource SpinnerSmall}" Visibility="Collapsed"/>
                    <Border Style="{StaticResource GlyphTile}" Width="46" Height="46">
                        <TextBlock Text="&#xE7E8;" Style="{StaticResource GlyphText}" FontSize="19"/>
                    </Border>
                    <StackPanel Margin="16,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="SummaryTitle" Text="Loading…" Style="{StaticResource H2}"/>
                        <TextBlock x:Name="SummarySub" Text="" Style="{StaticResource ItemDesc}" Margin="0,2,0,0"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <TextBlock x:Name="EmptyText" Text="No startup apps found." Style="{StaticResource Subtitle}" Visibility="Collapsed" Margin="4,8,0,0"/>

            <!-- Items -->
            <ItemsControl x:Name="EntryList" Focusable="False">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Row}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Icon: shell icon when available, gradient initial otherwise -->
                                <Grid Width="40" Height="40" VerticalAlignment="Center">
                                    <Border CornerRadius="11" Background="{StaticResource AccentGradient}">
                                        <TextBlock Text="{Binding Initial}" Foreground="White" FontWeight="Bold" FontSize="16"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <Border CornerRadius="11" Background="{StaticResource RaisedBrush}"
                                            Visibility="{Binding Source, ElementName=Ico, Converter={StaticResource NullToVis}}">
                                        <Image x:Name="Ico" Width="26" Height="26" RenderOptions.BitmapScalingMode="HighQuality"
                                               Source="{Binding ExePath, Converter={StaticResource ExeIcon}}"/>
                                    </Border>
                                </Grid>

                                <StackPanel Grid.Column="1" Margin="16,0,16,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource ItemTitle}"/>
                                    <TextBlock Text="{Binding Subtitle}" Style="{StaticResource ItemDesc}" Margin="0,2,0,0"/>
                                    <TextBlock Text="{Binding Command}" Style="{StaticResource Caption}" Margin="0,4,0,0"
                                               TextTrimming="CharacterEllipsis" ToolTip="{Binding Command}"/>
                                </StackPanel>

                                <Button Grid.Column="2" Style="{StaticResource IconButton}" Content="&#xE8B7;" ToolTip="Open file location"
                                        Margin="0,0,14,0" Click="OpenLocation_Click"
                                        Visibility="{Binding CanOpenLocation, Converter={StaticResource BoolToVis}}"/>

                                <CheckBox Grid.Column="3" Style="{StaticResource Switch}" IsChecked="{Binding IsEnabled, Mode=TwoWay}" Click="Toggle_Click"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
