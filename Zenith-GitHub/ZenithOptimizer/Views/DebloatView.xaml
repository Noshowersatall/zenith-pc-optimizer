<UserControl x:Class="Zenith.Views.DebloatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer Style="{StaticResource PageScroll}">
        <StackPanel Margin="0,0,4,32">
            <!-- Header -->
            <Grid Margin="0,4,0,24">
                <StackPanel Margin="0,0,340,0">
                    <TextBlock Text="Debloat" Style="{StaticResource H1}"/>
                    <TextBlock Text="Remove preinstalled apps you never asked for. They're removed for all users and won't come back for new accounts — you can always reinstall one from the Microsoft Store." Style="{StaticResource Subtitle}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button x:Name="SelectRecButton" Style="{StaticResource SecondaryButton}" Content="Select recommended" Click="SelectRecommended_Click"/>
                    <Button x:Name="RemoveButton" Style="{StaticResource PrimaryButton}" Margin="10,0,0,0" Click="Remove_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE74D;" FontFamily="{StaticResource IconFont}" FontSize="14" VerticalAlignment="Center"/>
                            <TextBlock x:Name="RemoveText" Text="Remove selected" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Loading -->
            <Border x:Name="LoadingCard" Style="{StaticResource Card}" Padding="24,20">
                <StackPanel Orientation="Horizontal">
                    <Control Style="{StaticResource Spinner}" Width="28" Height="28"/>
                    <TextBlock Text="Looking for installed bloatware…" Margin="16,0,0,0" VerticalAlignment="Center" FontSize="14"/>
                </StackPanel>
            </Border>

            <!-- Empty -->
            <Border x:Name="EmptyCard" Style="{StaticResource Card}" Padding="24,20" Visibility="Collapsed">
                <StackPanel Orientation="Horizontal">
                    <Border Width="40" Height="40" CornerRadius="20" Background="{StaticResource SuccessTintBrush}">
                        <TextBlock Text="&#xE73E;" FontFamily="{StaticResource IconFont}" Foreground="{StaticResource SuccessBrush}" FontSize="16"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="16,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Your PC is clean" Style="{StaticResource H2}"/>
                        <TextBlock Text="No known bloatware apps were found." Style="{StaticResource ItemDesc}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Items -->
            <ItemsControl x:Name="AppList" Focusable="False">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Row}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox Style="{StaticResource ModernCheck}" IsChecked="{Binding IsChecked, Mode=TwoWay}" Click="Item_Click"/>
                                <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                                    <WrapPanel>
                                        <TextBlock Text="{Binding Name}" Style="{StaticResource ItemTitle}" Margin="0,0,10,0"/>
                                        <Border Style="{StaticResource Badge}" Background="{StaticResource CyanTintBrush}"
                                                Visibility="{Binding Recommended, Converter={StaticResource BoolToVis}}">
                                            <TextBlock Text="Safe to remove" Style="{StaticResource BadgeText}" Foreground="{StaticResource CyanBrush}"/>
                                        </Border>
                                    </WrapPanel>
                                    <TextBlock Text="{Binding PackageName}" Style="{StaticResource Caption}" Margin="0,3,0,0"/>
                                    <TextBlock Text="{Binding Note}" FontSize="12" Foreground="{StaticResource WarningBrush}" Margin="0,4,0,0"
                                               TextWrapping="Wrap" Visibility="{Binding HasNote, Converter={StaticResource BoolToVis}}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
